def add(s1,s2);

add(s1, s2) begin:
    var retVal := s1;

    while(s2) begin:
        succ(retVal);
        pred(s2);
    end

    return retVal;
end

def mul(s1, s2) begin:
    var retVal := s1;
    pred(s2);

    while(s2) begin:
        retVal := add(retVal, s1);
        pred(s2);
    end

    return retVal;
end

def isZero(s1) begin:
    var retVal := 1;

    while(s1) begin:
        retVal := 0;
        s1 := 0;
    end

    return retVal;
end

def isNotZero(s1) begin:
    var retVal := 0;

    while(s1) begin:
        retVal := 1;
        s1 := 0;
    end

    return retVal;
end
 
var r0 := read();
var r1 := read();
var erg := isZero(r0);
write(erg);